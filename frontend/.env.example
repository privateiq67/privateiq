# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (Vercel): set to https://private-iq-backend.onrender.com
# Or rely on VERCEL build fallback in lib/api.ts
